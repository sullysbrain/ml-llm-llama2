# in prep for running llama2 on AWS, this is a test for uploading and running on an EC2 instance

import numpy as np

random_number = np.random.randint(100, size=10)

print(f'Random number: {random_number}')
